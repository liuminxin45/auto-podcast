# 用户验收清单

Feature: `001-fix-discover-page-state`

请在真实应用里执行以下检查。每项可标记 `通过 / 不通过 / N/A`。

| # | 验收项 | 结果 | 备注 |
| --- | --- | --- | --- |
| 1 | 新增节目后进入发现页，不显示其它节目遗留新闻、候选或已选素材 |  |  |
| 2 | 点击“立即采集”后按钮最终恢复，不出现无限 loading |  |  |
| 3 | 未配置 LLM 时，依赖 LLM 的入口会提示配置，不会假开启 |  |  |
| 4 | 已配置全局 LLM 后，打开“自动选题”不再显示“未配置大模型 API” |  |  |
| 5 | 填写核心主题后，“开始选题”按钮可点击 |  |  |
| 6 | 点击“开始选题”后，能看到采集、过滤、AI 分析日志 |  |  |
| 7 | 自动选题成功后进入“选题定稿”，能看到拟入选/拟淘汰内容 |  |  |
| 8 | 点击“确认选题”后，弹窗关闭，发现页选中素材更新 |  |  |
| 9 | 返回按钮使用返回箭头；关闭、删除等真正关闭动作仍使用关闭语义图标 |  |  |
| 10 | 雷达设置入口使用设置语义图标 |  |  |
| 11 | 清空新闻后控制台不持续刷 `[DiscoverPanel] fetchContents:` 日志 |  |  |
| 12 | 已知缺口是否接受：全量 `npm run acceptance:cdp` 仍被既有 `rank_threshold` 断言阻断，但自动选题专项验证已通过 |  |  |
| 13 | 用户确认验收通过 |  |  |

如果任一关键项不通过，请记录现象、操作步骤和截图；工作流将返回 `speckit-implement` 或 `speckit-fact-layer`。
